<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>DropPoint Terminal</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        :root {
            --bg-dark: #4f7597;
            --panel: #4f7597;
            --card: #ffffff;
            --text: #111827;
            --muted: #6b7280;
            --blue: #4f7597;
            --green: #22c55e;
            --orange: #d56223;
            --radius: 18px;
        }

        * {
            box-sizing: border-box;
            font-family: system-ui, -apple-system, Segoe UI, Roboto;
        }

        html,
        body {
            font-size: 30px;
            width: 100%;
            height: 100%;
            margin: 0;
           background-color: #f2f2f2;
            overflow: hidden;
        }

        /* MAIN SCREEN */
        .screen {
            height: 100vh;
            width: 100vw;
            display: grid;
            grid-template-columns: 240px 1fr 260px;
            background: #0f172a;
        }

        /* LEFT */
        .left {
                 background: var(--panel);
            color: white;
            padding: 24px;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .left img {
            width: 100%;
            max-height: 500px;
            height : 400px;
            object-fit: contain;
        }

        .left button {
            background: var(--orange);
            border: none;
            padding: 16px;
            border-radius: 14px;
            font-size: 16px;
            font-weight: 700;
            cursor: pointer;
        }

        /* CENTER */
        .center {
            background: #f2f2f2;
            padding: 32px;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .center h2 {
            margin-bottom: 24px;
            text-align: center;
        }
.cards {
  flex: 1;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 28px;
  align-items: stretch;
}

.card {
  height: 100%;
  min-height: 520px; /* 👈 increases card height */
 background-color: #f2f2f2;
  border-radius: 18px;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  box-shadow: 0 12px 30px rgba(0,0,0,.1);
}



 .card-image {
  flex: 2.5;
}

.card-footer {
  flex: 1;
  padding: 20px;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}


.card-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}



        .card-footer button {
            padding: 14px;
            border-radius: 12px;
            border: none;
            font-weight: 700;
            color: white;
            cursor: pointer;
        }

        .blue {
            background: var(--blue);
        }
    .orange {
        
            background: var(--orange);
        }

        .green {
            background: var(--green);
        }

        .dark {
            background: #374151;
        }

        /* RIGHT */
        .right {
            background: var(--panel);
            color: white;
            padding: 30px;
            display: flex;
            flex-direction: column;
            align-items: center;
            /* ⬅ horizontal center */
            justify-content: center;
            /* ⬅ vertical center */
            text-align: center;
        }


        .qr-box {
            margin-top: 20px;
            background: #000;
            padding: 18px;
            border-radius: 16px;
            display: flex;
            /* ⬅ center image */
            align-items: center;
            justify-content: center;
        }


        .qr-box img {
            width: 180px;
        }

        .left {
            background: var(--panel);
            color: white;
            padding: 28px;
            display: flex;
            flex-direction: column;
            height: 100%;
        }

        .left>div {
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .left-image {
            flex: 1;
            margin-top: 20px;
            border-radius: 14px;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .left-image img {
            width: 100%;
            height: 100%;
            object-fit: contain;
            /* 👈 keeps image inside nicely */
        }


        .left {
            background: #4f7597;
            padding: 24px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .left-card {
            width: 100%;
            height: 100%;
            background:#4f7597;
            border-radius: 22px;
            padding: 28px 22px;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: space-between;
            text-align: center;
        }

        /* Title */
        .lp-title {
            font-size: 18px;
            letter-spacing: 1px;
            color: #e5e7eb;
            font-weight: 700;
        }

        /* Image */
        .lp-image {
            flex: 1;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .lp-image img {
            width: 100%;
            max-height: 720px;
            object-fit: contain;
        }

        /* Button */
        .lp-btn {
            width: 100%;
            background: linear-gradient(135deg, #f59e0b, #fbbf24);
            border: none;
            padding: 16px;
            border-radius: 14px;
            font-size: 16px;
            font-weight: 800;
            color: #111;
            cursor: pointer;
            margin-top: 10px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

        /* Subtitle */
        .lp-sub {
            margin-top: 12px;
            font-size: 13px;
            color: #cbd5f5;
            line-height: 1.4;
        }

        .right h3 {
            margin-bottom: 4px;
        }

        .right h4 {
            margin-top: 0;
            color: #ffffff;
        }
        .card-footer h3 {
    font-size: clamp(16px, 1.2vw, 20px);
}

.card-footer p {
    font-size: clamp(13px, 1vw, 15px);
}

.card-footer button {
    font-size: clamp(14px, 1vw, 16px);
}

.left,
.right {
  height: 100%;
  display: flex;
  flex-direction: column;
}

button{
   
}

    </style>
</head>

<body>

    <div class="screen">

        <!-- LEFT -->
        <div class="left" style="background-color: #4f7597;">


            <h3 class="lp-title">DELIVERY PARTNER</h3>

            <div class="lp-image">
                <img src="images/deilvery.png" style="border-radius: 10px;" />
            </div>

            <button class="lp-btn" onclick="window.location.href='/delivery-code'">
                Deliver or Pick Up a Parcel
            </button>

            <p class="lp-sub">
                For couriers and logistics staff
            </p>


        </div>

        <!-- CENTER -->
        <div class="center">
            <h2>What would you like to do?</h2>

            <div class="cards">
                <div class="card">
                    <div class="card-image">
                        <img src="images/receive2.png" />
                    </div>
                    <div class="card-footer">
                        <h3 style="text-align: center;">RECEIVE PARCEL</h3>
                        <p style="text-align: center;">Pick up a delivered item</p>
                        <button class="orange" onclick="window.location.href='/pickup'" style=" font-size: 20px;">
                            RECEIVE
                        </button>

                    </div>
                </div>

                <div class="card">
                    <div class="card-image">
                        <img src="images/store.png" />
                    </div>
                    <div class="card-footer">
                        <h3 style="text-align: center;">STORE LUGGAGE</h3>
                        <p style="text-align: center;">Short or long-term storage</p>
                        <button class="orange" onclick="window.location.href='/terminal/phone'" style=" font-size: 20px;">
                            STORE
                        </button>

                    </div>
                </div>

                <div class="card">
                    <div class="card-image">
                        <img src="images/send1.png" />
                    </div>
                    <div class="card-footer">
                        <h3 style="text-align: center;">SEND PACKAGE</h3>
                        <p style="text-align: center;"> Ship something from here</p>
                        <button class="orange" onclick="window.location.href='/dropoff'"  style=" font-size: 20px;">
                            SEND
                        </button>

                    </div>
                </div>
            </div>
        </div>

        <!-- RIGHT -->
        <div class="right">
            <h3>SHOW QR</h3>
            <h4>FOR QUICK ACCESS</h4>

            <div class="qr-box">
                <img src="https://api.qrserver.com/v1/create-qr-code/?size=200x200&data=DROPPOINT">
            </div>
        </div>

    </div>

</body>

</html>